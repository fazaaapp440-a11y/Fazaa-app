# تقرير مرحلة تطبيق العميل Android | فزعة

**الحالة:** تنفيذ أولي إنتاجي على API الحقيقي، وليس شاشة Demo.  
**الهدف:** تمكين العميل من البحث والتواصل مع المهنيين قبل توسيع تدفقات المهني والإدارة.

## ما تم تنفيذه

تم استبدال شاشة الترحيب الأولية بتطبيق عميل يستهلك API فزعة الحقيقي. يستطيع المستخدم البحث عن المهنيين بالاسم أو الخدمة، التصفية بالمدينة، التصفية حسب التخصص، مشاهدة النتائج التي يعيدها الخادم، فتح الملف التفصيلي للمهني، رؤية حالة التوثيق والتقييم والخبرة والنبذة، والاتصال أو فتح واتساب عبر روابط الهاتف الحقيقية.

أُضيف تسجيل الدخول برقم الهاتف وكلمة المرور من خلال `/api/auth/login`، مع حفظ التوكن في Android SecureStore بدل التخزين النصي العادي. كما يستطيع المستخدم تسجيل الخروج وحذف التوكن المحلي.

تطبيق العميل لا ينشئ مهنيين وهميين ولا يعرض بيانات ثابتة؛ كل النتائج تأتي من API وقاعدة البيانات. وبحسب سياسة الخادم الحالية، لا تظهر في البحث إلا الحسابات النشطة والمهنيون المعتمدون ذوو الاشتراك النشط.

## التحقق

نجح `pnpm install --frozen-lockfile`، وفحص TypeScript لتطبيق الهاتف، و`expo prebuild` بعد إضافة SecureStore، و`git diff --check`. كما تم إعادة توليد مجلد Android بعد تغيير الإعداد الديناميكي.

## ما لم يُعتبر منجزًا بعد

هذه المرحلة لا تدّعي اكتمال تطبيق العميل بالكامل. ما زال مطلوبًا تنفيذ التسجيل الكامل، استعادة كلمة المرور، الملف الشخصي، التقييمات، الشكاوى، الإشعارات، معرض الأعمال الكامل، واختبارات UI على أجهزة Android فعلية. كما أن رابط Staging الافتراضي يجب استبداله بعنوان بيئة حقيقي قبل اختبار الشبكة.

## بوابة القبول التالية

لا تنتقل الحزمة التالية إلى الإعلانات أو الاشتراكات قبل إكمال تدفق العميل الأساسي: التسجيل، تسجيل الدخول، الاستعادة، البحث، الملف التفصيلي، الاتصال، واتساب، التقييم، الشكوى، والإشعارات، مع اختبار نجاح وفشل لكل تدفق على Staging حقيقي.
