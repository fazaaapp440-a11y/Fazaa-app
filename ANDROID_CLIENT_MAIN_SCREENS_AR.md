# تقرير واجهات العميل الرئيسية

**الحالة:** واجهات رئيسية مرتبطة بالـ API الحقيقي.  
**التاريخ:** 17 سبتمبر 2026

## الشاشات المنفذة

تم تنفيذ تبويب **الرئيسية** مع رسالة تعريفية واضحة عن فزعة، زر انتقال للاستكشاف، قائمة التخصصات القادمة من `/api/categories`، وقائمة الأعلى تقييمًا القادمة من `/api/providers/top-rated`.

تم تنفيذ تبويب **استكشف** مع بحث بالاسم أو الخدمة، تصفية بالمدينة، عرض نتائج API، حالات التحميل والخطأ، وبطاقات مهنيين تعرض التقييم والخبرة وحالة التوثيق وروابط الاتصال وواتساب.

تم الحفاظ على فتح الملف التفصيلي للمهني من البيانات الحقيقية عبر `/api/providers/:id`، مع عرض النبذة والتقييم والخبرة والتوثيق والاتصال المباشر.

تم تنفيذ تبويب **حسابي**، ويعرض بيانات المستخدم الحالي بعد المصادقة، أو دعوة لتسجيل الدخول وإنشاء الحساب للزائر، مع زر تسجيل الخروج وحذف الجلسة الآمنة.

## تجربة الاستخدام

تمت إضافة شريط تنقل سفلي بثلاثة تبويبات: الرئيسية، استكشف، حسابي. الواجهات مصممة باتجاه RTL، وتستخدم ألوان فزعة، وحالات فارغة وتحميل ورسائل خطأ، ولا تعتمد على بيانات ثابتة أو وظائف وهمية.

## التحقق

نجح `pnpm install --frozen-lockfile`، وفحص TypeScript لتطبيق الهاتف، و`expo prebuild --platform android --clean --no-install`، و`git diff --check`.

## الحدود الحالية

لا تزال شاشة التخصص في الرئيسية تنتقل إلى الاستكشاف دون تمرير معرف التخصص؛ هذه نقطة تحسين لاحقة حتى لا يتم تجاوز البحث الحالي. كما أن شاشة الملف التفصيلي ستُوسّع لاحقًا لعرض معرض الأعمال والتقييمات والشكاوى. يجب اختبار الشبكة وواجهات API على Staging حقيقي وعلى جهاز Android فعلي قبل اعتبار الإصدار جاهزًا للمتجر.
