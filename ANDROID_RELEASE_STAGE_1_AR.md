# تقرير المرحلة الأولى: إنشاء تطبيق Android لفزعة

**التاريخ:** 17 سبتمبر 2026  
**الحالة:** هيكل Android جاهز للمراجعة، ولم يُعلن بعد كإصدار Google Play نهائي.

## ما تم تنفيذه

تم إنشاء مشروع Expo/React Native حقيقي داخل `artifacts/mobile`، مع توليد مجلد Android الأصلي بواسطة `expo prebuild`. تم اعتماد اسم التطبيق `فزعة | FAZAAH`، وApplication ID ثابت هو `com.fazaa.app`، ونسخة أولى `1.0.0` و`versionCode=1`.

تم ضبط `compileSdkVersion=36` و`targetSdkVersion=36`، وتجهيز `eas.json` بملفي staging وproduction، بحيث يكون staging بصيغة APK للتوزيع الداخلي، وproduction بصيغة Android App Bundle (`.aab`) لقناة المتجر.

تمت إضافة هوية بصرية أولية تشمل الأيقونة والأيقونة التكيفية وSplash، وإنشاء شاشة أولية باللغة العربية توضّح رؤية فزعة: مهنيون موثقون وتواصل مباشر دون دفع للخدمات داخل التطبيق.

تم جعل رابط API ديناميكيًا عبر `app.config.ts`: Staging يستخدم رابط staging، وProduction يستخدم `EXPO_PUBLIC_API_URL` أو القيمة الافتراضية المخصصة للإنتاج. لا تُحفظ أسرار التوقيع أو كلمات المرور داخل المستودع.

## التحقق المنجز

نجح `pnpm install`، ونجح فحص TypeScript لتطبيق الهاتف، ونجح `expo config`، ونجح `expo prebuild`. تم التحقق من وجود `com.fazaa.app` و`versionCode=1` و`versionName=1.0.0` وTarget/Compile SDK 36 داخل ملفات Gradle.

## العائق الحالي

لم يتم بناء APK محليًا داخل بيئة العمل لأن Android SDK و`adb` غير مثبتين، و`ANDROID_HOME` غير مضبوط. هذا عائق في بيئة البناء فقط، وليس فشلًا في TypeScript أو إعداد Expo. يجب تنفيذ البناء الفعلي عبر EAS Build أو بيئة تحتوي Android SDK 36 قبل اعتبار نسخة Android قابلة للتثبيت والنشر.

## ما تبقى قبل Google Play

يجب تنفيذ EAS Build لإنتاج APK داخلي وAAB إنتاجي، اختبارهما على أجهزة Android فعلية، إعداد Play App Signing، إنشاء keystore خارج GitHub، إكمال حذف الحساب والبيانات والسياسات وData Safety، ثم اختبار التسجيل والتوثيق والبحث والاتصال والملفات والإشعارات والاشتراكات والإعلانات قبل التقديم.

## المخاطر

الأصول البصرية الحالية أولية وليست اعتمادًا نهائيًا للعلامة التجارية. كما أن الشاشة الحالية هي نقطة انطلاق تقنية وليست التطبيق الكامل لكل تدفقات العميل والمهني. لذلك لا ينبغي رفع AAB إلى Production أو الإعلان عن جاهزية Google Play قبل إكمال التدفقات والاختبارات المذكورة.
