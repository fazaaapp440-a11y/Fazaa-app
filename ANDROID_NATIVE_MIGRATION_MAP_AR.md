# خريطة تحويل فزعة من Web إلى Android Native

## المصدر المرجعي

المصدر الوحيد للمطابقة هو تطبيق الويب في `artifacts/sanad` وخادم الـAPI في `artifacts/api-server` مع مخططات قاعدة البيانات في `lib/db`. مشروع `artifacts/mobile` الحالي هو Expo/React Native وليس الناتج المطلوب، ولذلك لم يُعتبر أساسًا Native.

## الحالة المنفذة في هذه المرحلة

أُضيف مشروع مستقل في `artifacts/native` باستخدام Kotlin وJetpack Compose فقط. لا يعتمد على React أو Expo أو Capacitor أو WebView. طبقة `FazaaApi` تستعمل نفس مسارات `/api` الموجودة في الويب، وتتعامل مع البيانات الحقيقية بدل البيانات الوهمية.

| Web | Native الحالي | الحالة |
|---|---|---|
| `/` و`/home` | شاشة الرئيسية وبيانات `/categories` و`/providers` | منفذ أولي متصل بالـAPI |
| `/discover` و`/providers` | شاشة الاستكشاف والبحث والمدينة | منفذ أولي؛ الفلاتر المتقدمة لم تكتمل |
| `/providers/:id` | تفاصيل المهني والاتصال وواتساب | منفذ أولي؛ التقييمات والمعرض لم يكتمل عرضهما |
| `/login` | تسجيل الدخول بالهاتف وكلمة المرور | منفذ أولي؛ التخزين الآمن للتوكن غير مكتمل |
| `/profile` و`/settings` | تبويب الحساب وتسجيل الخروج | منفذ أولي |
| بقية صفحات العميل | لا توجد شاشة Native بعد | غير منفذة |
| صفحات المهني | لا توجد شاشة Native بعد | غير منفذة |
| صفحات الإدارة | خارج تطبيق العميل وتحتاج نطاقًا منفصلًا | غير منفذة |

## الوظائف التي يجب تنفيذها قبل إعلان الاكتمال

المصادقة الكاملة (التسجيل بالبريد والهاتف، OTP، استعادة كلمة المرور، Google إن كان مدعومًا)، الطلبات والطوارئ والرسائل والمكالمات والإشعارات والمفضلة والملف الشخصي والإعدادات والخصوصية والشروط، ثم تدفقات المهني كاملة (التسجيل، التحقق، رفع الوثائق، معرض الأعمال، الاشتراكات والإعلانات والأرباح والمحفظة)، واختبارات الصلاحيات للإدارة إن كان المطلوب تطبيق إدارة Native.

## قيود التحقق الحالية

بيئة التنفيذ الحالية لا تحتوي Android SDK أو `adb`، لذلك لا يمكن من هذه الجلسة إثبات تثبيت APK على Emulator أو جهاز فعلي. كما أن عنوان الـAPI في المشروع Native موضوع كقيمة بناء قابلة للضبط، ويجب تعيينه إلى بيئة Staging حقيقية قبل اختبار الاتصالات.

**الحكم:** هذه بداية تنفيذ Native حقيقية قابلة للتوسعة، وليست إعلانًا عن تحويل كامل أو مطابقة 100%. يجب إكمال المسارات أعلاه وبناء APK واختباره فعليًا قبل استخدام هذا الوصف.
