# وثيقة تسليم مشروع فزعة | FAZAAH

**الغرض من الوثيقة:** تسليم المشروع إلى مبرمج أو فريق تطوير أو نظام ذكاء اصطناعي آخر دون فقدان السياق.  
**تاريخ الحالة:** 17 سبتمبر 2026.  
**المستودع:** [absherbalsafar-debug/NewFazaaapp][1].  
**الفرع النشط:** `main`.  
**آخر Commit:** `0c5ceaa` — `feat: add real client home and navigation screens`.

> **الخلاصة التنفيذية:** يحتوي المشروع حاليًا على Backend وبيانات وقاعدة تشغيل وواجهة ويب إدارية وواجهة ويب سابقة، إضافة إلى مشروع Android/Expo بدأ يتحول إلى تطبيق عميل حقيقي. تم إغلاق مجموعة مهمة من مشاكل الأمان والبنية، وتم تنفيذ مصادقة العميل والواجهات الرئيسية والبحث عن المهنيين والتواصل معهم. المشروع **ليس جاهزًا بعد للنشر على Google Play**؛ لأن تطبيق العميل ما زال يحتاج إكمال تدفقات العميل والمهني والإدارة والاختبارات الفعلية وبناء AAB وتكوين بيئة Staging وخدمات OTP والبريد والتوقيع.

## 1. رؤية المنتج التي يجب عدم تغييرها

فزعة منصة لمهنيين موثقين. يبحث العميل عن مهني مناسب ثم يتواصل معه مباشرة بالاتصال أو واتساب. لا يوجد دفع مقابل الخدمة بين العميل والمهني داخل التطبيق. الإيرادات التجارية تأتي من اشتراكات المهنيين والإعلانات المدفوعة. توجد لوحة ويب مستقلة للإدارة، ولا ينبغي خلطها مع تطبيق العميل أو تطبيق المهني.

يجب أن يظهر المهني للعملاء فقط عندما يكون حسابه نشطًا، وتوثيقه معتمدًا، واشتراكه نشطًا. هذه القاعدة مطبقة في مسارات البحث الحالية في الخادم، ويجب الحفاظ عليها في أي تطوير لاحق.

## 2. الحالة الحالية في سطر واحد

| المجال | الحالة الحالية | الحكم |
|---|---|---|
| Backend وAPI | موجودان ويحتويان على مسارات المصادقة والمهنيين والتوثيق والإعلانات والاشتراكات والشكاوى والتقييمات والإشعارات والتخزين | قاعدة جيدة، تحتاج اختبارات تكامل وStaging حقيقي |
| قاعدة البيانات | مخططات Drizzle وترحيلات وتغييرات للتوثيق ومعرض الأعمال | موجودة، يجب تشغيلها رسميًا على Staging وProduction قبل الإطلاق |
| أمان P0 | تم تنفيذ تحسينات كلمات المرور وSESSION_SECRET وCORS وHelmet وRate Limiting ورفع الملفات وCI وStaging documentation | منفذ برمجيًا، لكن لا يُغلق إطلاقًا قبل اختبار بيئة نشر فعلية |
| لوحة الإدارة | موجودة وتحتوي على مركز تحقق ومراجعة المهنيين وصور الأعمال | تحتاج إكمال إدارة العملاء والشكاوى والاشتراكات والإعلانات والمحافظ والتقارير واختبار الصلاحيات |
| Android | مشروع Expo مع Android native generated وApplication ID `com.fazaa.app` | الهيكل جاهز، ولم يُنتج AAB أو APK قابل للتسليم بعد |
| تطبيق العميل | مصادقة، رئيسية، استكشاف، بحث، تصنيف، ملف مهني مختصر، اتصال وواتساب، حساب | مرحلة تنفيذ فعلية، وليست Demo، لكنها غير مكتملة للإطلاق |
| تطبيق المهني | توجد أجزاء وخدمات في النظام السابق، لكن لا توجد واجهة Android مكتملة مستقلة | يبدأ بعد إغلاق تدفقات العميل الأساسية |
| Google Play | تم تدقيق الفجوات وإعداد EAS وTarget SDK 36 | لم يتم النشر، ولم يتم إعداد Play App Signing أو Data Safety بشكل نهائي |

## 3. بنية المستودع

المشروع Monorepo يعمل بواسطة pnpm. أهم مكوناته هي:

| المسار | المسؤولية |
|---|---|
| `artifacts/api-server` | خادم Express وواجهات API والمصادقة والصلاحيات |
| `lib/db` | مخططات قاعدة البيانات وترحيلات Drizzle |
| `lib/api-zod` | عقود والتحقق من بيانات API المولدة بـ Zod |
| `lib/api-client-react` | عميل API للواجهات السابقة |
| `artifacts/sanad` | واجهة ويب العميل/المهني السابقة ومكونات العمل الحالية |
| `artifacts/sanad-admin` | لوحة الإدارة المستقلة |
| `artifacts/mobile` | تطبيق Android/Expo الحالي |
| `scripts` | أدوات الترحيلات والفحوصات المساعدة |
| `.github/workflows/ci.yml` | GitHub Actions للفحص والبناء والأمان |
| `docs/STAGING_DEPLOYMENT_AR.md` | طريقة فصل Staging عن Production |

نقطة تشغيل تطبيق Android الحالية هي `artifacts/mobile/App.tsx`. إعداد Expo الديناميكي في `artifacts/mobile/app.config.ts`، وإعداد EAS في `artifacts/mobile/eas.json`.

## 4. ما تم إنجازه بالتفصيل

### 4.1 الإصلاحات الأمنية والتقنية الحرجة

تم استبدال SHA-256 المستخدم سابقًا لكلمات المرور بحل إنتاجي يعتمد على `scrypt`، مع دعم ترقية الهاش القديم عند تسجيل الدخول. أُضيف شرط قوي لسر الجلسة، وتم توثيق ملفات البيئة ومنع رفع أسرار حقيقية إلى Git.

تم تقييد CORS، وإضافة Helmet، وإضافة Rate Limiting. كما تم تشديد رفع الملفات والتحقق من نوع الملف وملكيته والوصول إلى الملفات المحمية. أضيفت اختبارات كلمات المرور، وأضيف GitHub Actions للفحص، كما أضيف مشغل ترحيلات رسمي وتوثيق Staging.

تمت إزالة `debug.keystore` من GitHub بعد اكتشافه، وإضافة قواعد تمنع تتبع ملفات `*.keystore` و`*.jks`. لا يوجد مفتاح توقيع إنتاجي داخل المستودع.

### 4.2 مركز توثيق المهنيين

تم إنشاء مركز تحقق داخل لوحة الإدارة. يشمل مراجعة مستندات الهوية، اعتماد المهني، رفضه أو طلب التعديل، سجل التدقيق، وإجراءات حماية فتح الوثائق والصور. أضيف دور موظف التوثيق إلى الصلاحيات.

تمت إضافة مراجعة صور معرض الأعمال. لا تظهر صور الأعمال غير المعتمدة في العرض العام، ويمكن لموظف التوثيق اعتمادها أو رفضها مع سبب قرار المراجعة.

### 4.3 Android وExpo

تم إنشاء مشروع Expo/React Native حقيقي. الهوية الحالية هي:

```text
اسم التطبيق: فزعة | FAZAAH
Application ID: com.fazaa.app
Version: 1.0.0
Version Code: 1
compileSdkVersion: 36
targetSdkVersion: 36
```

تم إعداد قناتي Staging وProduction في `eas.json`. الإنتاج مصمم لإنتاج Android App Bundle، بينما Staging مصمم للتوزيع الداخلي. تمت إضافة الأيقونة وAdaptive Icon وSplash أولية، ويجب اعتماد الهوية البصرية النهائية قبل المتجر.

### 4.4 مصادقة العميل

تطبيق العميل يحتوي على شاشة تسجيل الدخول برقم الهاتف وكلمة المرور، وإنشاء حساب بالبريد، وإنشاء حساب عبر OTP، والتحقق من الهاتف، واستعادة كلمة المرور، وتعيين كلمة مرور جديدة.

المسارات المستخدمة هي:

```text
POST /api/auth/login
POST /api/auth/register/email
POST /api/auth/send-otp
POST /api/auth/verify-otp
POST /api/auth/forgot-password
POST /api/auth/reset-password
GET  /api/auth/me
```

يُحفظ التوكن في `expo-secure-store`. لا يُحفظ في AsyncStorage أو في نص عادي.

### 4.5 واجهات العميل الرئيسية

تمت إضافة ثلاثة تبويبات داخل Android:

- **الرئيسية:** رسالة المنتج، التخصصات، الأعلى تقييمًا.
- **استكشف:** بحث بالاسم أو الخدمة، فلترة بالمدينة والتخصص، عرض البطاقات.
- **حسابي:** حالة المستخدم، بياناته، الدخول، وتسجيل الخروج.

تستخدم الصفحة الرئيسية `/api/categories` و`/api/providers/top-rated`. يستخدم الاستكشاف `/api/providers`، ويفتح الملف التفصيلي `/api/providers/:id`. البطاقات تعرض الاتصال وواتساب الحقيقيين، ولا تعتمد على بيانات ثابتة.

## 5. سجل آخر التغييرات المهمة

| Commit | التغيير |
|---|---|
| `fe1223d` | إغلاق P0 للمصادقة وتقوية المنصة |
| `9673895` | تقوية مركز التحقق وسير العمل |
| `9399893` | تدقيق فجوات Android وGoogle Play |
| `a68dba0` | إنشاء مشروع Android وExpo وإعداد EAS |
| `725a7e2` | إزالة Android debug keystore ومنع مفاتيح التوقيع |
| `e073a3b` | بناء تدفق بحث العميل الحقيقي |
| `c594653` | إضافة شاشات مصادقة العميل |
| `0c5ceaa` | إضافة الرئيسية والاستكشاف والحساب والتبويب السفلي |

حالة المستودع عند إعداد هذا التقرير نظيفة، و`main` المحلي يطابق `origin/main` عند آخر تحقق.

## 6. ما تبقى قبل اعتبار تطبيق العميل جاهزًا

هذه هي الأعمال ذات الأولوية، بالترتيب:

### أ. إكمال العميل

يجب إكمال إنشاء الحساب بجميع حالات الفشل، واستعادة كلمة المرور برسالة بريد حقيقية، وتدفق الملف الشخصي، ومعرض الأعمال، وعرض التقييمات، وتقديم تقييم، وتقديم شكوى، واستقبال الإشعارات. يجب إضافة حالات انقطاع الشبكة وإعادة المحاولة وانتهاء الجلسة.

يجب تمرير معرف التخصص من جميع بطاقات التخصص إلى البحث بصورة موحدة، وإضافة صفحة نتائج كاملة مع ترقيم أو تحميل تدريجي. كما يجب إضافة اختبارات UI لكل تدفق أساسي.

### ب. تطبيق المهني

يجب إنشاء واجهات Android فعلية للمهني تشمل إنشاء الحساب، اختيار التخصص والخدمات، بيانات المدينة، رفع الهوية الأمامية والخلفية، الصورة الشخصية، صور الأعمال، متابعة حالة التوثيق، تعديل الملف، إدارة معرض الأعمال، الاشتراكات، الإعلانات، والإحصائيات.

لا يجوز إظهار المهني للجمهور قبل اعتماد التوثيق ووجود اشتراك نشط. يجب اختبار ذلك على مستوى API والواجهة والإدارة.

### ج. لوحة الإدارة

يجب إكمال الإدارة المنفصلة للمهنيين والعملاء والتخصصات والإعلانات والاشتراكات والمحافظ والشكاوى والموظفين والتقارير. كما يجب مراجعة RBAC واختبار أن كل موظف يرى الوظائف المسموحة له فقط.

### د. الاشتراكات والإعلانات

مسارات الاشتراكات والإعلانات موجودة في الخادم، لكن يجب ربطها بواجهة عملية، ومزود دفع أو آلية تحصيل معتمدة، وسياسة تجديد وانتهاء واضحة. يجب ربط مقاييس المشاهدات والنقرات والاتصال وواتساب بتقارير مفيدة.

يجب عدم إضافة دفع للخدمات بين العميل والمهني؛ الدفع التجاري يخص الاشتراكات والإعلانات فقط وفق رؤية المنتج.

### هـ. المحافظ الإلكترونية

المطلوب واجهة إدارة المحافظ من لوحة التحكم مع شعارات معتمدة وبيانات قابلة للتغيير. لا ينبغي الادعاء بوجود ربط دفع مباشر قبل توقيع اتفاقية المزود وتنفيذ التكامل واختباره.

### و. البنية التشغيلية

يجب إنشاء بيئة Staging حقيقية بقاعدة بيانات منفصلة، وعنوان API حقيقي، ومفاتيح سرية مستقلة، وملفات تخزين مستقلة. يجب تنفيذ الترحيلات على Staging أولًا ثم إنتاج نسخة احتياطية قبل Production.

يجب ربط OTP برسائل SMS حقيقية، وربط استعادة كلمة المرور بمزود بريد حقيقي. لا يجوز استخدام الرموز التي يعيدها الخادم في وضع التطوير عند الإطلاق.

## 7. ما يمنع إعلان الجاهزية لـ Google Play حاليًا

لم يتم إنتاج APK أو AAB نهائي في بيئة تحتوي Android SDK. المحاولة المحلية فشلت لأن البيئة لا تحتوي `ANDROID_HOME` أو `adb`. نجاح `expo prebuild` لا يعني أن ملف AAB جاهز.

ما زال مطلوبًا إعداد Play App Signing وإدارة keystore خارج GitHub. كما يجب إنشاء سياسة الخصوصية وشروط الاستخدام، وتنفيذ حذف الحساب وحذف البيانات، وإكمال نموذج Data Safety، وإعداد رابط الدعم، وتجهيز لقطات الشاشة ووصف المتجر.

يجب اختبار Release على أجهزة Android فعلية، وليس الاكتفاء بفحص TypeScript أو prebuild. يجب أيضًا فحص الأداء، التشغيل دون شبكة، صلاحيات الإشعارات، فتح الهاتف وواتساب، رفع الملفات، وتوافق RTL.

## 8. من أين يبدأ المبرمج أو الذكاء الاصطناعي الآخر؟

يجب أن يبدأ من هذا المستودع بعد استنساخه، ثم يقرأ هذا التقرير والوثائق المرتبطة به. بعد ذلك ينفذ الخطوات التالية بالترتيب:

```bash
gh repo clone absherbalsafar-debug/NewFazaaapp
cd NewFazaaapp
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm --filter @workspace/fazaa-mobile run typecheck
```

ثم يراجع هذه الملفات أولًا:

```text
artifacts/mobile/App.tsx
artifacts/mobile/app.config.ts
artifacts/mobile/eas.json
artifacts/api-server/src/routes/auth.ts
artifacts/api-server/src/routes/providers.ts
artifacts/api-server/src/routes/verification.ts
artifacts/api-server/src/routes/storage.ts
lib/db/src/schema/providers.ts
lib/db/src/schema/provider_verification.ts
lib/db/src/schema/portfolio.ts
artifacts/sanad-admin/src/pages/verification-center.tsx
```

بعد الفحص، لا يبدأ بإضافة ميزة تجارية جديدة. البداية الصحيحة هي تنفيذ **بوابة إكمال العميل**: الملف الشخصي، معرض الأعمال، التقييمات، الشكاوى، الإشعارات، وحالات الشبكة. كل جزء يجب أن يمر بهذه الدورة:

1. قراءة عقد API وقاعدة البيانات الحالية.
2. تنفيذ الواجهة المرتبطة بالـ API الحقيقي.
3. إضافة حالات التحميل والفشل والفراغ.
4. إضافة اختبار أو سيناريو قبول واضح.
5. تشغيل TypeScript وprebuild والاختبارات.
6. تحديث تقرير الحالة.
7. إنشاء Commit مستقل ورفعه إلى `main` أو فتح فرع واضح.

يجب ألا يُعاد تصميم الرؤية التجارية أو إدخال الدفع بين العميل والمهني. يجب ألا تُستخدم بيانات وهمية أو روابط مؤقتة في النسخة التي ستدخل اختبار Staging.

## 9. أوامر التحقق المتوقعة بعد كل حزمة

```bash
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm --filter @workspace/fazaa-mobile run typecheck
cd artifacts/mobile
pnpm exec expo config --json
pnpm exec expo prebuild --platform android --clean --no-install
```

لبناء Android فعليًا، يجب استخدام بيئة تحتوي Android SDK أو EAS:

```bash
cd artifacts/mobile
eas build --profile staging --platform android
eas build --profile production --platform android
```

لا تُشغّل أمر Production قبل وضع الأسرار وبيانات API والتوقيع في مدير أسرار مناسب، وبعد مراجعة `eas.json` و`app.config.ts`.

## 10. معايير قبول الإصدار الأول

لا يُقبل الإصدار الأول إلا عندما يستطيع عميل جديد إنشاء حساب أو الدخول، البحث عن مهني معتمد، تصفية النتائج، فتح ملف المهني، رؤية الأعمال والتقييمات، الاتصال أو فتح واتساب، تقديم تقييم وشكوى، واستقبال الإشعارات.

ويجب أن يستطيع مهني جديد إنشاء حساب، رفع ملفه، متابعة التوثيق، إدارة أعماله، معرفة حالة اشتراكه وإعلانه، ومشاهدة إحصائياته. ويجب أن يستطيع موظف الإدارة مراجعة كل ذلك من لوحة مستقلة مع سجل تدقيق وصلاحيات واضحة.

كل ذلك يجب أن ينجح على Staging ببيانات حقيقية اختبارية، ثم على Release Android موقّع، مع عدم وجود أسرار في GitHub وعدم اعتماد وظائف مخفية أو Demo.

## 11. الوثائق السابقة المفيدة

- [المراجعة التقنية العامة](./FAZAAH_REVIEW_2026-09-17_AR.md).
- [التدقيق التشغيلي والتجاري](./FAZAAH_OPERATIONAL_COMMERCIAL_AUDIT_2026-09-17_AR.md).
- [الرؤية الرسمية للمنتج](./FAZAAH_OFFICIAL_PRODUCT_VISION_AR.md).
- [تدقيق فجوة Android وGoogle Play](./GOOGLE_PLAY_ANDROID_GAP_AUDIT_AR.md).
- [تقرير مرحلة Android](./ANDROID_RELEASE_STAGE_1_AR.md).
- [تقرير تطبيق العميل](./ANDROID_CLIENT_STAGE_1_AR.md).
- [تقرير المصادقة](./ANDROID_AUTH_STAGE_1_AR.md).
- [تقرير الواجهات الرئيسية](./ANDROID_CLIENT_MAIN_SCREENS_AR.md).
- [دليل فصل Staging عن Production](./docs/STAGING_DEPLOYMENT_AR.md).
- [سياسة الدفع التجاري](./COMMERCIAL_PAYMENT_POLICY_AR.md).

## 12. الخلاصة النهائية للمسلّم الجديد

المشروع ليس فكرة أولية فقط؛ توجد قاعدة Backend وقاعدة بيانات وميزات توثيق وإدارة وأمان، ويوجد تطبيق Android بدأ العمل عليه فعليًا. آخر ما تم إنجازه هو تطبيق عميل Android يحتوي على المصادقة والصفحة الرئيسية والاستكشاف والحساب والبحث الحقيقي والتواصل.

لكن المشروع ليس منتجًا جاهزًا للنشر التجاري. المرحلة التالية الصحيحة هي إكمال وظائف العميل واختبارها على Staging، ثم إنشاء تطبيق المهني، ثم إكمال لوحة الإدارة والاشتراكات والإعلانات، ثم تنفيذ متطلبات Google Play وبناء Release فعلي. أي مطور جديد يجب أن يحافظ على هذه الأولويات وألا يقفز إلى ميزات تجارية قبل إغلاق تدفقات العميل والأمان والتشغيل.

## References

[1]: https://github.com/absherbalsafar-debug/NewFazaaapp "FazaaH GitHub repository"
