# تقرير حزمة مصادقة تطبيق العميل

**الحالة:** مصادقة حقيقية مرتبطة بخادم فزعة، وليست واجهات Demo.  
**التاريخ:** 17 سبتمبر 2026

## المسارات المنفذة

تمت إضافة شاشة تسجيل الدخول برقم الهاتف وكلمة المرور باستخدام `/api/auth/login`، وشاشة إنشاء حساب بالبريد الإلكتروني باستخدام `/api/auth/register/email`، مع اختيار الدور `client` وعدم إنشاء ملف مهني من تطبيق العميل.

تمت إضافة تدفق إنشاء الحساب والتحقق عبر الهاتف باستخدام `/api/auth/send-otp` ثم `/api/auth/verify-otp`. يدعم التدفق تسجيل دخول المستخدم الموجود أو إكمال تسجيل مستخدم جديد بالاسم.

تمت إضافة استعادة كلمة المرور باستخدام `/api/auth/forgot-password` ثم إعادة التعيين عبر `/api/auth/reset-password`. في بيئة التطوير يعيد الخادم رمز الاختبار كما هو مصمم حاليًا، بينما لا يعيده في الإنتاج.

## حماية الجلسة

بعد نجاح الدخول أو التسجيل، يُحفظ التوكن في `expo-secure-store`، وليس في AsyncStorage أو نص عادي. عند تشغيل التطبيق يُستعاد التوكن ويُتحقق من `/api/auth/me`، ويُحذف محليًا عند فشل التحقق. يدعم التطبيق تسجيل الخروج المحلي.

## تجربة الاستخدام

تشمل الشاشات:

- تسجيل الدخول.
- إنشاء حساب عميل.
- إنشاء الحساب عبر الهاتف.
- إدخال OTP وإعادة الإرسال.
- استعادة كلمة المرور.
- تعيين كلمة مرور جديدة.
- رسائل أخطاء ونجاح باللغة العربية.
- RTL وحقول هاتف وبريد وكلمة مرور.

## التحقق

نجح `pnpm install --frozen-lockfile`، وفحص TypeScript لتطبيق الهاتف، و`expo prebuild --platform android --clean --no-install`، و`git diff --check`.

## ملاحظات قبل الإنتاج

يجب ربط إرسال OTP بخدمة رسائل فعلية قبل الإنتاج؛ الخادم الحالي يسجل الرمز ويعيده فقط خارج Production. كما يجب ربط استعادة كلمة المرور بمزود بريد فعلي وعدم الاعتماد على رمز الاستجابة التطويري. ويجب اختبار جميع التدفقات على Staging حقيقي وعلى جهاز Android فعلي قبل قبولها كبوابة Google Play.
